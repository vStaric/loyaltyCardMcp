{
  "name": "loyaltycard-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Tolar MCP server — peer core: identity, envelope crypto, REST client",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "bin": {
    "tolar-mcp": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:write": "prettier --write ."
  },
  "dependencies": {
    "libsodium-wrappers": "^0.7.15",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/libsodium-wrappers": "^0.7.14",
    "@types/node": "^22.10.2",
    "@types/qrcode": "^1.5.5",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1",
    "vitest": "^4.1.11"
  }
}
